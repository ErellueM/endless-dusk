[tool.gdlint]
# Die maximale Zeilenlänge anpassen
line-length = 200

# Hier zählst du alle Regeln auf, die ignoriert werden sollen:
disable = [
    "class-definitions-order",
    "unused-argument",
    "no-elif-return",
    "no-else-return",
]